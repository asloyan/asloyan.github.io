/* Set the background color for the entire page */
body {
  background-color: #FEF9E7;
}

/* Set the font family and font size for the entire page */
body {
  font-family: Arial, sans-serif;
  font-size: 16px;
}

/* Add a background image to the header, and set its size and position */
header {
  background-image: url("MannheimSky.jpg");
  background-size: cover;
  background-position: center;
}

/* Set the height of the header */
header {
  height: 250px;
}

/* Center the title within the header */
h1 {
  text-align: center;
  margin-top: 80px;
}

/* Style the navigation menu */
nav {
  background-color: #333;
  overflow: hidden;
  display: flex; /* add this line */
  justify-content: space-between; /* add this line */
}

/* Set the links in the navigation menu to be white and to have a padding and margin */
nav a {
  display: block;
  color: white;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  margin: 0;
}

/* Style the links in the navigation menu on hover */
nav a:hover {
  background-color: #ddd;
  color: black;
}

/* Style the active link in the navigation menu */
.active {
  background-color: #4CAF50;
}

/* Create a horizontal line underneath the header */
hr {
  border: none;
  height: 1px;
  color: #333;
  background-color: #333;
  margin-top: 0;
}

/* Set the padding for the main content */
main {
  padding: 20px;
}

/* Set the font size and line height for headings */
h2 {
  font-size: 28px;
  line-height: 1.3;
}

/* Set the font size and line height for paragraphs */
p {
  font-size: 18px;
  line-height: 1.6;
}

/* Style the sections on the pages */
section {
  margin-top: 40px;
}

/* Set the width and height of the banner image */
.banner {
  width: 100%;
  height: 250px;
  background-image: url("MannheimSky.jpg");
  background-size: cover;
  background-position: center;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
}

/* Style the text in the banner */
.banner-text {
  font-size: 48px;
  color: white;
  text-shadow: 2px 2px 4px #000000;
  margin-bottom: 20px;
}

/* Style the banner links */
.banner-links {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Set the link styles for the banner */
.banner-links a {
  color: white;
  font-size: 24px;
  margin: 0 20px;
  text-decoration: none;
  text-shadow: 2px 2px 4px #000000;
}

/* Style the banner links on hover */
.banner-links a:hover {
  color: #cccccc;
}

/* Set the style for the banner items */
.banner-item {
  flex: 1;
  text-align: center;
}
